/*
 * include.c
 *
 *  Created on: 17/05/2016
 *      Author: jdani
 */


void save_signal( SIGNAL * FIR_signal, char * filename)
{
	FILE * pFilt;
	int i;
	pFilt = fopen(filename,"wb");

	int dato;
	for(i=0;i<(FIR_signal->lenght);i++)
	{
		dato=(int)FIR_signal->signal[i];
		fwrite (&dato, sizeof(int), 1, pFilt);
	}
	fclose(pFilt);
}
